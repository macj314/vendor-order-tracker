@{
  Layout = "_Layout";
}

@using VendorOrderTracker.Models;

<h1>Vendors</h1>

@if (@Model.Count == 0)
{
  <h4>You have no vendors...</h4>
}

@foreach (Vendor vendor in Model)
{
  <h4><a href="/vendors/@vendor.Id">@vendor.Name</a></h4>
}

<button><a href="/">Home</a></button>